class UserClass(var name: String, var password: String, var age: Int, var adminStatus : Boolean?, var accountStatus : String ) {
    var userName = name
    var userPassword = password
    var userAge =  age
    var admin = adminStatus
    var accountType = accountStatus
}